#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main() {
    long int cardNum = get_long("Number: ");
    int length = 0;

    for (long int x = 1; x <= cardNum; pow(x, 10))
    {
        length += 1;
        if (
    }
}
