#include <cs50.h>
#include <stdio.h>

int stair;

int main(void)
{
    int height = get_int("What's the height? ");

    for (int x = 1; x <= height; x++)
    {
        stair
    }

}

int stair(height, x)
{
    for (int y = 1; y <= x; y++)
    {
        printf("#");
    }
}
