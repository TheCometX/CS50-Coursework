#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int height = get_int("What's the height");

    for (int x = height - 1; x > 0; x++)
    {
        if ()
        for (int y = )
        {
        }
    }
}
