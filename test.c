#include <stdio.h>

int main()
{
    int x = 1;
    int y = 2 * 1;

    if (x < y)
    {
        printf("%d is smaller than %d", x, y);
    }
    int t = 0;
    for ( ; t <= 2; t++)
    {
        printf("yes\n");
    }
}
