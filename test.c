#include <stdio.h>

int main()
{
    int x = 1;
    int y = 2;

    if (x < y)
    {
        printf("%d is smaller than %d", x, y);
    }
    for (int t = 0; t <= 2; t++)
    {
        printf("yes\n");
    }
}
