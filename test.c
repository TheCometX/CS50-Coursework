#include <stdio.h>
#include <cs50.h>

int main(void)
{
  long cardNum = get_long("Number? \n");
  int times = 0
  for (int x = 10; x < cardNum; x * 10)
  {
    times += 1;
    if (carNum % 2 == 0)
    {
      if (timed)
    }
  }
}
